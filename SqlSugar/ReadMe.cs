namespace SqlSugarInter
{
    public class ReadMe
    {
        //DBContent 继承sqlsugar 里面的SimpleClient  基本语法

        //repository.GetById();                             --根据id获取实体类
        //repository.GetByIdAsync();                        --异步根据id获取实体类
        //repository.GetFirst();                            --根据查询条件获取第一条
        //repository.GetFirstAsync();                       --异步根据查询条件获取第一条
        //repository.GetList();                             --获取集合
        //repository.GetListAsync();                        --异步获取集合
        //repository.GetPageList();                         --获取分页集合
        //repository.GetPageListAsync();                    --异步获取分页集合

        //repository.Insert();                                      --插入
        //repository.InsertAsync();                                 --异步插入
        //repository.InsertRange();                                 --批量插入
        //repository.InsertRangeAsync();                            --批量异步插入
        //repository.InsertOrUpdate();                              --插入或修改
        //repository.InsertOrUpdateAsync();                         --异步插入或修改
        //repository.InsertReturnEntity();                          --插入完成返回实体类
        //repository.InsertReturnEntityAsync()                      --插入完成返回实体类
        //repository.InsertReturnIdentity();                        --插入返回自增值
        //repository.InsertReturnBigIdentityAsync();                --异步插入返回自增值

        //repository.Delete();                                      --删除
        //repository.DeleteAsync();                                 --异步删除
        //repository.DeleteById();                                  --根据id删除
        //repository.DeleteByIdAsync();                             --根据id异步删除
        //repository.DeleteByIds();                                 --批量根据id删除
        //repository.DeleteByIdsAsync();                            --批量根据id异步删除

        //repository.Update();                                      --更新
        //repository.UpdateAsync()                                  --异步更新
        //repository.UpdateRange();                                 --范围更新
        //repository.UpdateRangeAsync();                            --异步范围更新
    }
}